<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
    <select id="count" resultType="int">
        SELECT count(*) FROM tb_board
    </select>

    <delete id="deleteAll">
        DELETE FROM tb_board
    </delete>

    <delete id="delete" parameterType="map">
        DELETE FROM tb_board WHERE bno = #{bno} and writer = #{writer}
    </delete>

    <insert id="insert" parameterType="com.springmvc.domain.BoardDto">
        INSERT INTO tb_board
            (title, content, writer)
        VALUES
            (#{title}, #{content}, #{writer})
    </insert>

    <select id="selectAll" resultType="com.springmvc.domain.BoardDto">
        SELECT bno, title, content, writer, view_cnt, comment_cnt, reg_date
        FROM tb_board
        ORDER BY reg_date DESC, bno DESC
    </select>

    <sql id="selectFromBoard">
        SELECT bno, title, content, writer, view_cnt, comment_cnt, reg_date
        FROM tb_board
    </sql>

    <select id="select" parameterType="int" resultType="com.springmvc.domain.BoardDto">
        <include refid="selectFromBoard"/>
        WHERE bno = #{bno}
    </select>

    <select id="selectPage" parameterType="map" resultType="com.springmvc.domain.BoardDto">
        <include refid="selectFromBoard"/>
        ORDER BY reg_date DESC, bno DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <update id="update" parameterType="com.springmvc.domain.BoardDto">
        UPDATE tb_board
        SET   title = #{title}
          , content = #{content}
          , up_date = now()
        WHERE bno = #{bno}
    </update>

    <update id="updateCommentCnt" parameterType="map">
        UPDATE tb_board
        SET   comment_cnt = comment_cnt + #{cnt}
        WHERE bno = #{bno}
    </update>

    <update id="increaseViewCnt" parameterType="int">
        UPDATE tb_board
        SET   view_cnt = view_cnt + 1
        WHERE bno = #{bno}
    </update>

</mapper>
